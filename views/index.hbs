<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carnival Animal - Inicio</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link href="/css/index-styles.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=forum" />
</head>
<body>
  <div class="container-fluid min-vh-100 d-flex flex-column">
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <img src="/img/logo2.png" alt="HOME" width="40" height="40">
        </a>
        <span class="navbar-text mx-auto h3 text-primary">Carnival Animal</span>
     <a href="/pages/chatbot.html" class="me-3 text-decoration-none text-primary">
            <span class="material-symbols-outlined">forum</span>
          </a>
        <div class="dropdown">
          <button class="btn btn-link dropdown-toggle text-primary" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fas fa-user"></i>
          </button>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
            <li><a class="dropdown-item" href="/logout">Cerrar sesión</a></li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="flex-grow-1">
      <div class="container my-4">
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
          {{#each activities}}
            <div class="col">
            <div class="card h-100">
                <img src="{{this.image}}" class="card-img-top">
                <div class="card-body">
                <h5 class="card-title">{{this.title}}</h5>
                <a href="/pages/intro{{this.id}}.html" class="btn btn-primary w-100">Empezar la actividad</a>

                </div>
            </div>
            </div>
          {{/each}}
        </div>
      </div>
    </main>

    <footer class="text-center py-3">
      <p class="text">&copy; 2024 Carnival Animal. Todos los derechos reservados.</p>
    </footer>
  </div>

  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
